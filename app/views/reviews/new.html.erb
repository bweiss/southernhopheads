<% provide(:title, "New Review") %>

<div class="post">
  <h2 class="clearfix title">New Review</h2>
  <p class="clearfix meta"></p>
  <div class="entry">
    <%= simple_form_for [@beer, @review] do |f| %>
      <%= f.error_notification %>
      <%= display_base_errors @review %>
      <%= render 'fields', :f => f %>
      <%= f.button :submit, "Submit", :class => 'btn-primary' %>
    <% end %>
  </div>
</div>
